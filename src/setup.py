import setuptools

setuptools.setup(
    name='complex_event_video',
      version='0.1',
      description='',
      author='',
      author_email='',
      url='',
      packages=setuptools.find_packages(),
      install_requires=[]
)